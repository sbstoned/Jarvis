# Generated Projects

Jarvis uses this directory as its runtime workspace for generated and repaired projects.

The contents are intentionally excluded from Git because they may contain:

- user projects
- build artifacts
- dependency caches
- checkpoints
- uploaded project files
- private/generated data

Only this README and .gitkeep are tracked.
