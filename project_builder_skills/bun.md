# Bun
Use Bun only when selected/requested. Keep package.json/bun lock state coherent, use `bun install`, project scripts and `bun test` as appropriate. Do not mix npm/pnpm/yarn lockfiles or Node-specific runtime assumptions without a deliberate compatibility reason.
