# F# / .NET
Use SDK-style .fsproj projects and preserve F# compilation order. Keep modules/types in dependency order and use `dotnet restore/build/test`. Prefer idiomatic immutable domain modeling while respecting requested .NET/UI frameworks. Tests should be real .NET test projects or correctly included test files.
