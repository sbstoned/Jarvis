# Clojure
Use the selected Clojure build convention consistently (deps.edn/Clojure CLI or Leiningen). Keep namespaces aligned with file paths, separate pure/domain logic from effects, and use native test tooling. Do not mix unrelated JVM build conventions unless the repository explicitly requires it.
