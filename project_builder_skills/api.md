# API Engineering
Keep transport DTOs, validation, domain/service logic and persistence responsibilities separated. Define stable request/response/error contracts before consumers. Add tests for success, validation, missing resources and failure paths. For REST/GraphQL/gRPC, use the selected framework's native routing/schema/build tools and do not invent incompatible conventions.
