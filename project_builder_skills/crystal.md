# Crystal
Use shard.yml consistently when dependencies exist. Keep require paths and module/type names coherent, compile with `crystal build` and test with `crystal spec`. Treat compile-time type errors as authoritative and repair the owning API rather than suppressing checks.
