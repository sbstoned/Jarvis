# Data Import / Export
For CSV or similar interchange, define explicit schema/headers, parsing/serialization rules, validation, duplicate/update policy, encoding and error reporting. Keep import/export contracts consistent with persisted/domain models. Test round-trips, malformed input, missing columns and escaping. UI file picking and backend parsing may be separate owners in mixed repositories.
