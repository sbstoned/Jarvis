# Django
Keep settings, apps, models, migrations, URLs, views/services and templates/API layers coherent. Generate and validate migrations when models change. Use Django's native test runner or configured pytest-django. Never claim persistence works without migration/database tests appropriate to the project.
