# Secure Engineering
Apply ordinary secure defaults: validate untrusted input, avoid hard-coded secrets, use least privilege, parameterized data access and safe path handling. Security checks should support the requested product rather than silently changing its purpose. Never disable build/tests or validation to hide a security-related failure.
