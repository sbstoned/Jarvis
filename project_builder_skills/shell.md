# Shell / Bash
Write noninteractive, fail-visible scripts with quoted variables, predictable exit codes and explicit dependencies. Use syntax checks and project-local test scripts. Avoid destructive host operations and fragile global state. Treat shell scripts as real source with tests/config ownership, not glue that bypasses the command broker.
