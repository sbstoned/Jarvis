# FastAPI
Use typed request/response models, dependency injection and explicit validation/error behavior. Keep persistence/service logic out of route handlers when the project is nontrivial. Test routes with the framework's test client and run Python static/import checks. Respect async boundaries and lifecycle/context-manager contracts.
