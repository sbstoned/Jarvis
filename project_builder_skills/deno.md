# Deno
Use deno.json/deno.jsonc, native imports/tasks and Deno permissions intentionally. Type-check with `deno check` and test with `deno test`. Avoid Node-only package/config assumptions unless compatibility mode is explicitly part of the project. Keep permission requirements minimal and documented.
