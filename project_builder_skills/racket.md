# Racket
Keep module paths, language declarations and package metadata coherent. Compile with `raco make` and test with `raco test`. Prefer explicit contracts/tests for public module boundaries and do not invent conventions from unrelated Lisp ecosystems.
