# PowerShell
Prefer PowerShell 7 (`pwsh`) for cross-platform scripts unless Windows PowerShell is explicitly required. Use strict parameter validation, predictable exit codes and noninteractive behavior for automation. Test modules/scripts with Pester when requested. Avoid machine-wide destructive changes unless the task explicitly requires and authorizes them.
