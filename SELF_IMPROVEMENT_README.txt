JARVIS v2.15 SELF IMPROVEMENT

Say "Jarvis, improve yourself" to launch an OpenAI coding agent in the background.
It targets the current Jarvis project, checks Git status first, makes one safe
local improvement, validates it, and does not commit/push/deploy by default.

Hardware temperatures now bypass WMI and directly load:
tools\LibreHardwareMonitor\LibreHardwareMonitorLib.dll

Run TEST_HARDWARE_SENSORS.bat to see exactly which real temperature sensors your
PC exposes. CPU/GPU temps should show when present. RAM temp only appears when
the DIMMs or motherboard expose a memory temperature sensor.
